"""Stage 1: HILDA+ smoothing, upscaling, and chain processing scripts.

These are standalone scripts (not part of the landsymm package) that
process raw HILDA+ GeoTIFF data into half-degree NetCDF files suitable
for the remapping stage.
"""
